# Week10 runner

`run_articulated_task.py` is a thin entry point to
`week9_note.scripts.run_articulated_task`. Keeping only one implementation
ensures the four accepted scenes use identical execution, validation, summary,
and rendering logic.
